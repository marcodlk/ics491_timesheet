# ics491_timesheet

# clone the git repo, then use the following commands to compile and run the code:

# change directory
cd src/
# ensure permission
chmod +x compile.sh
chmod +x run.sh
# compile
./compile.sh
# run
./run.sh
